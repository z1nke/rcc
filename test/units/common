#include <stdio.h>
#include <stdlib.h>

void assert(int expected, int actual, char *code, int line) {
  if (expected == actual) {
    printf("%s => %d\n", code, actual);
  } else {
    printf("%s => %d expected but got %d in %d\n", code, expected, actual, line);
    exit(1);
  }
}

// [75] Support file-scope functions
static int static_fn() { return 5; }

// [116] Add extern
int ext1 = 5;
int *ext2 = &ext1;

// [117] Handle extern declarations in a block
int ext3 = 7;
int ext_fn1(int x) { return x; }
int ext_fn2(int x) { return x; }
